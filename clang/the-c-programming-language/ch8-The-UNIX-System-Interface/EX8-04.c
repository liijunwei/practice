/*
page 157

标准库函数 `int fseek(FILE *fp, lone offset, int origin)` 类似于函数 `lseek`, 所不同的是, 该函数中的fp
是一个文件指针而不是文件描述符, 且返回值是一个int类型的状态而非位置值.

编写函数fseek, 并确保该函数与库中其他函数使用的缓冲能够协同工作

*/

#include <stdio.h>

int main(int argc, char const *argv[])
{

  return 0;
}
